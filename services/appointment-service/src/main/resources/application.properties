spring.application.name=appointment-service
server.port=${SERVER_PORT:8086}

# Database Configuration - Appointment Service uses appointmentsdb
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3312/appointmentsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123456}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.default_schema=appointmentsdb

# ===================== MAIL CONFIG =====================
# FE sẽ lấy email từ user-service và gọi xuống đây,
# appointment-service chỉ cần cấu hình SMTP để gửi mail.
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:johncuong1522@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ryga ddny ryyf jpcr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# ===================== EXTERNAL SERVICES =====================
# Hospital Service URL (thông qua API Gateway hoặc trực tiếp)
services.hospital.url=${HOSPITAL_SERVICE_URL:http://localhost:8084}